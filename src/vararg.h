/* Autogenerated by vararg_h */

int TIFFSetField_TIFFTAG_ARTIST (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_ARTIST (TIFF* tif);

int TIFFSetField_TIFFTAG_BADFAXLINES (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_BADFAXLINES (TIFF* tif);

int TIFFSetField_TIFFTAG_BITSPERSAMPLE (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_BITSPERSAMPLE (TIFF* tif);

int TIFFSetField_TIFFTAG_CLEANFAXDATA (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_CLEANFAXDATA (TIFF* tif);

int TIFFSetField_TIFFTAG_COMPRESSION (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_COMPRESSION (TIFF* tif);

int TIFFSetField_TIFFTAG_CONSECUTIVEBADFAXLINES (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_CONSECUTIVEBADFAXLINES (TIFF* tif);

int TIFFSetField_TIFFTAG_COPYRIGHT (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_COPYRIGHT (TIFF* tif);

int TIFFSetField_TIFFTAG_DATETIME (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_DATETIME (TIFF* tif);

int TIFFSetField_TIFFTAG_DOCUMENTNAME (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_DOCUMENTNAME (TIFF* tif);

int TIFFSetField_TIFFTAG_FAXMODE (TIFF* tif, int arg1);

int TIFFGetField_TIFFTAG_FAXMODE (TIFF* tif);

int TIFFSetField_TIFFTAG_FILLORDER (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_FILLORDER (TIFF* tif);

int TIFFSetField_TIFFTAG_GROUP3OPTIONS (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_GROUP3OPTIONS (TIFF* tif);

int TIFFSetField_TIFFTAG_GROUP4OPTIONS (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_GROUP4OPTIONS (TIFF* tif);

int TIFFSetField_TIFFTAG_HOSTCOMPUTER (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_HOSTCOMPUTER (TIFF* tif);

int TIFFSetField_TIFFTAG_IMAGEDESCRIPTION (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_IMAGEDESCRIPTION (TIFF* tif);

int TIFFSetField_TIFFTAG_IMAGEDEPTH (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_IMAGEDEPTH (TIFF* tif);

int TIFFSetField_TIFFTAG_IMAGELENGTH (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_IMAGELENGTH (TIFF* tif);

int TIFFSetField_TIFFTAG_IMAGEWIDTH (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_IMAGEWIDTH (TIFF* tif);

int TIFFSetField_TIFFTAG_INKNAMES (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_INKNAMES (TIFF* tif);

int TIFFSetField_TIFFTAG_INKSET (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_INKSET (TIFF* tif);

int TIFFSetField_TIFFTAG_JPEGQUALITY (TIFF* tif, int arg1);

int TIFFGetField_TIFFTAG_JPEGQUALITY (TIFF* tif);

int TIFFSetField_TIFFTAG_JPEGCOLORMODE (TIFF* tif, int arg1);

int TIFFGetField_TIFFTAG_JPEGCOLORMODE (TIFF* tif);

int TIFFSetField_TIFFTAG_JPEGTABLESMODE (TIFF* tif, int arg1);

int TIFFGetField_TIFFTAG_JPEGTABLESMODE (TIFF* tif);

int TIFFSetField_TIFFTAG_MAKE (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_MAKE (TIFF* tif);

int TIFFSetField_TIFFTAG_MATTEING (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_MATTEING (TIFF* tif);

int TIFFSetField_TIFFTAG_MAXSAMPLEVALUE (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_MAXSAMPLEVALUE (TIFF* tif);

int TIFFSetField_TIFFTAG_MINSAMPLEVALUE (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_MINSAMPLEVALUE (TIFF* tif);

int TIFFSetField_TIFFTAG_MODEL (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_MODEL (TIFF* tif);

int TIFFSetField_TIFFTAG_ORIENTATION (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_ORIENTATION (TIFF* tif);

int TIFFSetField_TIFFTAG_PAGENAME (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_PAGENAME (TIFF* tif);

int TIFFSetField_TIFFTAG_PHOTOMETRIC (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_PHOTOMETRIC (TIFF* tif);

int TIFFSetField_TIFFTAG_PLANARCONFIG (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_PLANARCONFIG (TIFF* tif);

int TIFFSetField_TIFFTAG_PREDICTOR (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_PREDICTOR (TIFF* tif);

int TIFFSetField_TIFFTAG_PRIMARYCHROMATICITIES (TIFF* tif, float* arg1);

float* TIFFGetField_TIFFTAG_PRIMARYCHROMATICITIES (TIFF* tif);

int TIFFSetField_TIFFTAG_REFERENCEBLACKWHITE (TIFF* tif, float* arg1);

float* TIFFGetField_TIFFTAG_REFERENCEBLACKWHITE (TIFF* tif);

int TIFFSetField_TIFFTAG_RESOLUTIONUNIT (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_RESOLUTIONUNIT (TIFF* tif);

int TIFFSetField_TIFFTAG_ROWSPERSTRIP (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_ROWSPERSTRIP (TIFF* tif);

int TIFFSetField_TIFFTAG_SAMPLEFORMAT (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_SAMPLEFORMAT (TIFF* tif);

int TIFFSetField_TIFFTAG_SAMPLESPERPIXEL (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_SAMPLESPERPIXEL (TIFF* tif);

int TIFFSetField_TIFFTAG_SMAXSAMPLEVALUE (TIFF* tif, double arg1);

double TIFFGetField_TIFFTAG_SMAXSAMPLEVALUE (TIFF* tif);

int TIFFSetField_TIFFTAG_SMINSAMPLEVALUE (TIFF* tif, double arg1);

double TIFFGetField_TIFFTAG_SMINSAMPLEVALUE (TIFF* tif);

int TIFFSetField_TIFFTAG_SOFTWARE (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_SOFTWARE (TIFF* tif);

int TIFFSetField_TIFFTAG_STONITS (TIFF* tif, double arg1);

double TIFFGetField_TIFFTAG_STONITS (TIFF* tif);

int TIFFSetField_TIFFTAG_SUBFILETYPE (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_SUBFILETYPE (TIFF* tif);

int TIFFSetField_TIFFTAG_TARGETPRINTER (TIFF* tif, char* arg1);

char* TIFFGetField_TIFFTAG_TARGETPRINTER (TIFF* tif);

int TIFFSetField_TIFFTAG_THRESHHOLDING (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_THRESHHOLDING (TIFF* tif);

int TIFFSetField_TIFFTAG_TILEDEPTH (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_TILEDEPTH (TIFF* tif);

int TIFFSetField_TIFFTAG_TILELENGTH (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_TILELENGTH (TIFF* tif);

int TIFFSetField_TIFFTAG_TILEWIDTH (TIFF* tif, uint32 arg1);

uint32 TIFFGetField_TIFFTAG_TILEWIDTH (TIFF* tif);

int TIFFSetField_TIFFTAG_XRESOLUTION (TIFF* tif, float arg1);

float TIFFGetField_TIFFTAG_XRESOLUTION (TIFF* tif);

int TIFFSetField_TIFFTAG_WHITEPOINT (TIFF* tif, float* arg1);

float* TIFFGetField_TIFFTAG_WHITEPOINT (TIFF* tif);

int TIFFSetField_TIFFTAG_YCBCRCOEFFICIENTS (TIFF* tif, float* arg1);

float* TIFFGetField_TIFFTAG_YCBCRCOEFFICIENTS (TIFF* tif);

int TIFFSetField_TIFFTAG_YCBCRPOSITIONING (TIFF* tif, uint16 arg1);

uint16 TIFFGetField_TIFFTAG_YCBCRPOSITIONING (TIFF* tif);

int TIFFSetField_TIFFTAG_YPOSITION (TIFF* tif, float arg1);

float TIFFGetField_TIFFTAG_YPOSITION (TIFF* tif);

int TIFFSetField_TIFFTAG_YRESOLUTION (TIFF* tif, float arg1);

float TIFFGetField_TIFFTAG_YRESOLUTION (TIFF* tif);

